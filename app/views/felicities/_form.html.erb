<%= form_with(model: felicity, local: true) do |form| %>
  <% if felicity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(felicity.errors.count, "error") %> prohibited this felicity from being saved:</h2>

      <ul>
      <% felicity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :activity %>
    <%= form.text_field :activity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
