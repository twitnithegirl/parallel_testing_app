<%= form_with(model: joy, local: true) do |form| %>
  <% if joy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(joy.errors.count, "error") %> prohibited this joy from being saved:</h2>

      <ul>
      <% joy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :activity %>
    <%= form.text_field :activity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
