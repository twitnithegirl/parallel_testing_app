<%= form_with(model: malice, local: true) do |form| %>
  <% if malice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(malice.errors.count, "error") %> prohibited this malice from being saved:</h2>

      <ul>
      <% malice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :activity %>
    <%= form.text_field :activity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
